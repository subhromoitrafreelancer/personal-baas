<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  {{> theme_init}}
  <title>Users — Personal BaaS Admin</title>
  <link rel="stylesheet" href="/admin/static/css/admin.css" />
  <link rel="stylesheet" href="/admin/static/css/admin-users.css" />
</head>
<body>
  {{> topbar}}

  <main class="page admin-users-page">
    <div class="page-header">
      <div class="page-header-text">
        <h1>Users</h1>
        <p class="page-description">Application end users, scoped to the selected project.</p>
      </div>
      <div class="page-header-actions">
        <label class="field">Project <select id="project-select"></select></label>
        <span id="users-status" class="run-status" role="status"></span>
      </div>
    </div>

    <div class="admin-users-toolbar toolbar">
      <input id="search-input" type="search" placeholder="Search by email…" />
      <button id="search-btn" type="button" class="btn btn-outline" data-icon="search">Search</button>
      <button type="button" id="show-bulk-create-btn" class="btn btn-primary" data-icon="add">Add users</button>
    </div>

    <div id="secret-banner" class="secret-banner" hidden></div>

    <form id="bulk-create-user-form" class="inline-form bulk-create-form" hidden>
      <label class="field bulk-create-field">
        One user per line — <code>email</code> or <code>email,password</code> (blank password auto-generates)
        <textarea id="bulk-user-input" rows="6" placeholder="alice@example.com&#10;bob@example.com,s3cret123"></textarea>
      </label>
      <div class="bulk-create-actions">
        <button type="submit" class="btn btn-primary" data-icon="save">Create</button>
        <button type="button" id="cancel-bulk-create-btn" class="btn btn-outline" data-icon="close">Cancel</button>
      </div>
    </form>

    <div class="admin-users-filters toolbar">
      <label class="field">Email contains <input id="filter-email" type="search" placeholder="Filter loaded rows…" /></label>
      <label class="field">Status
        <select id="filter-status">
          <option value="">Any</option>
          <option value="active">Active</option>
          <option value="disabled">Disabled</option>
        </select>
      </label>
      <label class="field">MFA
        <select id="filter-mfa">
          <option value="">Any</option>
          <option value="enabled">Enabled</option>
          <option value="disabled">Disabled</option>
        </select>
      </label>
      <span class="filter-hint">Filters the <strong>loaded page</strong> only — not a server search.</span>
    </div>

    <div class="table-wrap">
      <table class="table users-table">
        <thead>
          <tr>
            <th>User</th>
            <th>Status</th>
            <th>Email verified</th>
            <th>MFA</th>
            <th>Created</th>
            <th>Last sign-in</th>
            <th></th>
          </tr>
        </thead>
        <tbody id="users-tbody"></tbody>
      </table>
    </div>

    <div class="pagination">
      <button id="prev-page-btn" type="button" class="btn btn-outline btn-sm">Previous</button>
      <span id="page-info"></span>
      <button id="next-page-btn" type="button" class="btn btn-outline btn-sm">Next</button>
    </div>
  </main>

  {{> footer}}

  <script src="/admin/static/js/icons.js"></script>
  <script src="/admin/static/js/toast.js"></script>
  <script src="/admin/static/js/topbar.js"></script>
  <script src="/admin/static/js/clipboard.js"></script>
  <script src="/admin/static/js/project-selector.js"></script>
  <script src="/admin/static/js/admin-users.js"></script>
</body>
</html>
